# 策略完善旧版字段状态机吸收审计 v0.1

> 分线：10_工程化交接 / 旧知识体系吸收融入  
> 状态：v0.1 / TP-010 首个循环切片执行产物  
> 输入旧资料：`策略完善_v0.2/策略字段表_v0.1.md`、`策略完善_v0.2/策略状态机规则_v0.2.md`、`策略完善_v0.2/人工样本标注模板_v0.1.md`、`策略完善_v0.2/MQL5观察指标需求文档_v0.1.md`  
> 对齐真源：`07_样本标注/样本字段表_v0.1.md`、`07_样本标注/样本标注模板_v0.1.md`、`08_指标需求/MQL5观察指标需求_v0.2.md`、`08_指标需求/MQL5观察指标字段映射与模块优先级_v0.1.md`、`09_规则与回测/状态机规则整合_v0.1.md`  
> 目的：把旧版策略字段、状态机、样本模板、MQL5 观察器需求与当前真源逐项对齐，避免后续循环代理重复造轮子或漏掉旧知识。

---

## 1. 本轮循环代理目标

```text
目标：执行 TP-010 首个主题切片，把 `策略完善_v0.2` 中与字段、状态机、样本模板、MQL5观察器相关的旧知识吸收进当前项目理解，并给出 absorbed / superseded / needs_validation / rejected 分类。

完成态：本审计文件落地，索引/变更记录更新，validate_all 通过。
```

本轮不是：

```text
1. 不重写所有当前真源文件。
2. 不批量移动旧目录。
3. 不把 v0.1 阈值升级成已验证规则。
4. 不把观察器需求升级成自动交易 EA。
```

---

## 2. 输入旧资料摘要

### 2.1 `策略字段表_v0.1.md`

旧字段组：

```text
A 基础行情字段
B 第一控盘区字段
C Market Profile / Volume Profile 字段
D 第一波拉升字段
E 深回撤字段
F POC 回收字段
G AVWAP 字段
H/I/J 二次确认字段
K 成交量确认字段
L 风险否决字段
M 最终分类字段
```

判断：

```text
大部分已被当前 `07_样本标注/样本字段表_v0.1.md` 和 `08_指标需求/MQL5观察指标需求_v0.2.md` 吸收，但字段命名已升级：
control_zone_* → fcz_* / first_control_zone_*
impulse_* → first_impulse_*
poc_price → poc_level
vah_price / val_price → vah_level / val_level
pullback_* → retracement / deep_washout / sweep 组合字段
```

### 2.2 `策略状态机规则_v0.2.md`

旧状态：

```text
S0 无结构
S1 第一控盘区形成
S2 第一波拉升完成
S3 深回撤测试
S4 重新接近 / 站回 POC
S5 重新接受 AVWAP
S6 第一次拉起
S7 回撤不破关键成本锚
S8 二次拉升确认
S9 稳定小段观察区
SX 失效 / 诱多 / 尾仓派发风险
```

判断：

```text
已被 `09_规则与回测/状态机规则整合_v0.1.md` 升级吸收为更明确的 STATE_* 状态机。
旧 S 状态适合保留为 legacy alias，方便旧样本/旧文档对照，但不应作为当前主状态命名。
```

### 2.3 `人工样本标注模板_v0.1.md`

旧模板强项：

```text
1. 人工复盘结构清晰。
2. 包含截图路径、原始数据路径、样本最终分类、风险审查。
3. 可读性强，适合人工填写。
```

判断：

```text
已被当前 `07_样本标注/样本标注模板_v0.1.md` 扩展吸收，并增强了：
- 事前可见原则
- 失败不得剔除
- A/B/C/D/E/F 样本分类
- 执行成本字段
- 反证意义
- 审计字段
```

### 2.4 `MQL5观察指标需求文档_v0.1.md`

旧需求强项：

```text
1. 明确指标不是自动下单 EA。
2. 模块完整：FCZ、Profile、POC/VAH/VAL、AVWAP、Fib 深回撤、结构、风险、状态、导出。
3. 给出了 MQL5 input 参数草案。
```

判断：

```text
已被 `08_指标需求/MQL5观察指标需求_v0.2.md` 和 `08_指标需求/MQL5观察指标字段映射与模块优先级_v0.1.md` 大幅吸收。
但旧 v0.1 中“输入参数设计”仍有价值，当前 v0.2 更偏模块/字段，没有完整保留 input 参数清单；应在未来 MQL5 MVP 计划中恢复为参数表。
```

---

## 3. 吸收分类矩阵

| 旧知识项 | 当前状态 | 目标真源 | 分类 | 说明 |
|---|---|---|---|---|
| 基础行情字段 symbol/timeframe/OHLC/volume/spread/atr | 当前样本字段表部分吸收 | `07_样本标注/样本字段表_v0.1.md` | absorbed | 当前字段更适合样本/审计，OHLC 细字段未全部进入样本表，后续可留给原始数据 |
| control_zone_* | 已升级为 fcz_* / first_control_zone_* | `07_样本标注/样本字段表_v0.1.md`、`08_指标需求/MQL5观察指标需求_v0.2.md` | absorbed | 命名已规范化 |
| control_zone_quality weak/medium/strong | 当前使用 score/confidence | 样本字段表 / 指标需求 | superseded | 当前分数化更适合 validator / CSV |
| profile_bin_size / profile_type / value_area_ratio | 当前只保留 profile_bin_count 等关键字段 | `08_指标需求/MQL5观察指标字段映射与模块优先级_v0.1.md` | needs_validation | 未来 MQL5 MVP 参数表应恢复 bin/value_area_ratio |
| poc_price / vah_price / val_price | 已升级为 poc_level / vah_level / val_level | 多个真源 | absorbed | 命名统一 |
| distribution_shape | 当前样本字段表未显性保留 | `07_样本标注/样本字段表_v0.1.md` 或未来观察器字段 | needs_validation | 对区间结构有用，但是否必填需样本验证 |
| impulse_* 第一波字段 | 已升级为 first_impulse_* | 样本字段表 / 状态机 | absorbed | 当前命名更清晰 |
| impulse_quality climax | 当前转入 distribution_score / risk | 风险评分 | absorbed | 不再作为独立枚举 |
| pullback_detected / pullback_low_* | 当前拆成 retracement / deep_washout / sweep | 样本字段表 / 状态机 | absorbed | 当前更贴合深洗/扫流动性语义 |
| 0.618-0.786 / 0.886 阈值 | 当前仍作为语义参考 | 状态机 / 指标需求 | needs_validation | 保留为初始观察区，不应当作已验证阈值 |
| POC reclaim weak/medium/strong | 当前升级为 poc_acceptance_state | 样本字段表 / 指标需求 | absorbed | 状态更细 |
| AVWAP anchor types | 当前增加多锚点尊重评分 | 样本字段表 / 指标需求 | absorbed | v0.2 更完整 |
| avwap_slope | 当前未作为核心字段 | 未来观察器字段 | needs_validation | 可能用于趋势/成本斜率，但需样本验证 |
| first_bounce / second_push / BOS | 当前已吸收 | 样本字段表 / 指标需求 / 状态机 | absorbed | 命名基本延续 |
| volume_confirmation | 当前分散在 volume ratios / risk | 样本字段表 | partially_absorbed | 当前未单列 volume_confirmation 枚举，后续可评估是否需要 |
| distribution_score / bull_trap_risk | 当前升级为 death_market_score / bull_trap_score / liquidity_trap_score | 风险字段 | absorbed | 风险维度更细 |
| final_classification | 当前保留并扩展 | 样本字段表 | absorbed | 当前分类更审计化 |
| S0-SX 状态机 | 当前升级为 STATE_* | 状态机规则整合 | superseded_with_alias | 旧状态应作为 legacy alias，不作为当前主状态 |
| MQL5 input 参数草案 | 当前 v0.2 未完整保留 | 未来 MQL5 MVP 计划 | needs_validation | 建议在 MVP 计划中作为参数表恢复 |
| 图表绘制需求 | 当前 v0.2 吸收 | MQL5观察指标需求 v0.2 | absorbed | 已覆盖模块需求 |
| 数据导出模块 | 当前保留为 CSV/JSON 导出 | 指标需求 / 字段映射 | absorbed | 后续需和样本 validator 绑定 |

---

## 4. 当前真源是否需要立即修改

### 4.1 不建议立即改动当前主真源

原因：

```text
当前 07/08/09 真源已经大部分吸收旧版内容，且命名、审计字段、反证字段比旧版更成熟。
贸然批量 patch 可能引入重复字段和旧阈值回流。
```

### 4.2 建议只回填三个“待吸收提醒”

#### A. MQL5 MVP 计划必须恢复旧版 input 参数表

旧 v0.1 有完整 input 参数草案：

```text
InpLookbackBars
InpATRPeriod
InpUseTickVolume
InpMinControlZoneBars
InpMaxControlZoneATR
InpMinZoneVolumeRatio
InpProfileBins
InpValueAreaRatio
InpMinImpulseZoneMultiple
InpMinImpulseVolumeRatio
InpDeepRetracementMin
InpDeepRetracementMax
InpDangerRetracement
InpWeakAcceptanceBars
InpMediumAcceptanceBars
InpReclaimTimeoutBars
InpShowAVWAPZoneStart
InpShowAVWAPImpulseStart
InpShowAVWAPReclaim
InpSecondPushTolerance
InpMinSecondPushVolumeRatio
InpStrongSecondPushVolumeRatio
InpMaxDistributionScore
InpExtremeDistributionScore
```

建议：

```text
未来 `08_指标需求/MQL5观察指标MVP开发计划_v0.1.md` 必须包含参数表，来源标注为旧 v0.1 absorbed_candidate。
```

#### B. legacy 状态 alias 应写入未来状态机 v0.2

建议建立映射：

| 旧状态 | 当前状态 |
|---|---|
| S0 无结构 | STATE_IDLE / STATE_NO_TRADE |
| S1 第一控盘区形成 | STATE_FCZ_CANDIDATE / STATE_FCZ_LOCKED |
| S2 第一波拉升完成 | STATE_FIRST_IMPULSE |
| S3 深回撤测试 | STATE_DEEP_WASHOUT_OR_SWEEP |
| S4 站回 POC | STATE_RECLAIM_TEST |
| S5 接受 AVWAP | STATE_COST_ANCHOR_ACCEPTED |
| S6 第一次拉起 | STATE_PULLBACK_TEST 前置反应 |
| S7 回撤不破关键成本锚 | STATE_PULLBACK_TEST |
| S8 二次拉升确认 | STATE_SECOND_START_CANDIDATE |
| S9 稳定小段观察区 | STATE_CONFIRMED_OBSERVATION |
| SX 失效/诱多/派发 | STATE_FAILED_RECLAIM / STATE_DEATH_MARKET_RISK / STATE_NO_TRADE |

建议：

```text
未来状态机 v0.2 增加 legacy_alias_map，便于旧样本/旧文档迁移。
```

#### C. volume_confirmation 是否恢复为独立字段需样本验证

旧版有：

```text
volume_confirmation = none / weak / valid / excessive
```

当前字段更细，但没有总枚举。

建议：

```text
暂不恢复为核心字段；在 FCZ_B/C/D 真实样本补齐后再判断是否需要一个 aggregate_volume_confirmation。
```

---

## 5. 本轮结论

```text
1. `策略完善_v0.2` 的字段、状态机、样本模板、MQL5 需求不是废弃物，而是当前 07/08/09 真源的重要前身。
2. 当前真源已经吸收了大部分旧知识，并在反证、审计、GMGN、执行成本、状态路径方面更成熟。
3. 旧版仍有三个未完全吸收的价值点：MQL5 input 参数表、legacy S 状态映射、volume_confirmation 总字段。
4. 本轮不建议直接 patch 07/08/09 主真源；建议把这些作为后续具体文件的输入，避免旧阈值污染当前主线。
```

---

## 6. 下一轮自动切片

按照循环代理方法，不停在建议，下一轮应自动进入：

```text
TP-010-B：MQL5 MVP 参数表回填准备
```

候选产物：

```text
08_指标需求/MQL5观察指标MVP开发计划_v0.1.md
```

但在写 MVP 计划前，仍需先完成更靠前的工程质量门：

```text
TP-003：index reference validator
```

当前自动选择规则：

```text
如果目标是“旧知识吸收继续推进”：执行 TP-010-B。
如果目标是“减少项目长期漂移”：执行 TP-003。
```

基于 owner 最新批评“不要一做一停”，推荐继续自动执行 TP-010-B，除非遇到真实 owner decision blocker。
```

---

## 7. 本轮验证要求

```text
1. 本文件已新增。
2. 知识库索引已引用本文件。
3. 变更记录记录本阶段。
4. validate_all 通过。
5. git commit / push / CI 通过。
```
