# 可一次性跑完任务长循环队列 2026-06-17

> 分线：10_工程化交接 / A端长任务队列  
> 状态：v0.1 / 非样本收集任务批处理计划  
> 目的：先把无需外部样本收集、无需 owner 新授权、可通过文档/模板/状态回填完成的任务一次性跑完；再进入需要 B端收样本证据的 TP-005 / TP-006。  
> 边界：不自动交易、不接 API key/private key、不创建批量 issue、不合并 PR、不做外部样本判定。

---

## 1. A端分类结论

当前剩余任务按执行属性分两类：

### A类：可以一次性跑完 / 本轮执行

```text
LQ-001：任务包状态回填
  把已经实际完成的 TP-003 / TP-004 标记为已完成。

LQ-002：TP-007 MQL5 观察器 MVP 计划状态回填
  该文件已存在，应标记为“已完成初版 / 后续实现需 owner”。

LQ-003：TP-009 需求子协议激活证据模板化
  已有 issue #4、handoff、源样例路径；可先落一个 reusable template 文件。

LQ-004：长循环队列本身落库
  明确哪些先跑、哪些等待样本收集，防止再次误把样本任务当文档任务完成。

LQ-005：索引与变更记录同步
  将新增模板、队列文件、A/B/E 补审文件纳入索引和变更记录。
```

### B类：不能一次性跑完 / 需要样本收集

```text
TP-005：FCZ_B 真实失败样本
  需要真实外部样本、GMGN raw refs、图表证据、失败判定。

TP-006：FCZ_C/D 图表派生结构
  需要图表/原始数据派生判断；可利用已有 raw refs，但不能凭空填 unknown。

TP-010：旧知识体系吸收融入循环代理
  需要逐个旧资料包做 B端 source pack 和 C端映射，不应和本轮状态回填混跑。
```

---

## 2. 本轮执行模式

本轮不是继续扩理论，而是：

```text
A端：分类与 stop condition
B端：确认哪些任务无需外部样本
C端：补模板/状态逻辑
D端：落库与索引更新
E端：validate_all / index validator / PR CI
```

---

## 3. LQ-001：任务包状态回填

状态：本轮执行。

证据：

```text
scripts/validate_index_references.py exists
tests/test_validate_index_references.py exists
scripts/validate_sample_records.py exists
tests/test_validate_sample_records.py exists
python scripts/validate_all.py reports:
  OK: index references valid
  OK: sample markdown records valid
```

处理：

```text
TP-003：已完成
TP-004：已完成
```

---

## 4. LQ-002：TP-007 MQL5 观察器 MVP 计划状态回填

状态：本轮执行状态回填，不再重写计划。

证据：

```text
08_指标需求/MQL5观察指标MVP开发计划_v0.1.md 已存在。
该文件已经明确：观察器 / 状态标注器 / 人工复盘辅助工具；不是自动下单 EA。
```

处理：

```text
TP-007 标记为“已完成初版 / 后续 MQL5 代码实现需另拆并需 owner 边界确认”。
```

---

## 5. LQ-003：TP-009 需求子协议激活证据模板化

状态：本轮执行。

已有证据：

```text
GitHub Issue #4：https://github.com/sunshihao001/meme-/issues/4
Handoff：10_工程化交接/Codex_handoff_demand_subprotocol_activation_template.md
源样例：specs/mql5-fcz-reclaim-model/clarification/demand_cognition_orchestrator_activation.md
源报告：specs/mql5-fcz-reclaim-model/reports/loop_agent_demand_grilling_report_20260615_0117.md
```

本轮产物：

```text
10_工程化交接/templates/demand_subprotocol_activation_evidence_template_v0.1.md
```

边界：

```text
不创建独立 Hermes skill。
不声称外部 Superpowers / grill-me / grill-with-docs 已作为本地 skill 执行。
只把 activation evidence 变成项目内可复用模板。
```

---

## 6. 本轮不执行项

```text
TP-005：需要真实样本收集。
TP-006：需要图表派生证据，不凭空填 unknown。
TP-010：需要逐轮旧知识 source pack，不和状态回填混跑。
PR merge：需要 owner。
GitHub issue 批量创建：需要 owner。
```

---

## 7. 完成标准

本轮完成必须满足：

```text
1. 长循环队列文件落库。
2. TP-007 状态回填。
3. TP-009 template 落库。
4. 索引与变更记录更新。
5. python scripts/validate_all.py 通过。
6. commit / push 当前 PR 分支。
7. 最终明确列出需要样本收集的下一队列。
```

---

## 8. A端 stop condition

完成本轮后停止，不继续自动进入样本搜索。

原因：

```text
样本收集任务需要 B端外部证据，不应和本轮“可一次性跑完”的内部文档/模板任务混在一起。
```
