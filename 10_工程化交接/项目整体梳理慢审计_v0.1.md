# 项目整体梳理慢审计 v0.1

> 分线：10_工程化交接 / 项目结构慢审计  
> 状态：v0.1 / 慢审计草案  
> 方法：按 V0.5 需求拷问端流程执行；只处理一个最高价值问题；不做快节奏重构。  
> 本轮唯一问题：当前项目里哪些已经完善过的部件没有被接入整体体系，导致 owner 需要靠记忆提醒 Agent 才能找到；应该如何用最小、非侵入方式建立一张“项目结构—功能—文件—状态—缺口”的慢审计图？

---

## 1. 本轮执行边界

```text
run_mode：manual
loop_type：runtime_audit + project_structure_audit
allowed_side_effects：write_files + update_index_changelog + validate
forbidden_capabilities：交易执行、私钥、swap、自动下单、cron、重构目录、批量移动历史文件
max_iterations：1
```

本轮不做：

```text
1. 不重构目录。
2. 不批量搬迁旧文件。
3. 不把 V0.5 方法论全文复制进业务仓库。
4. 不创建真实 GitHub issue。
5. 不进入 Codex。
6. 不继续扩写交易理论。
```

---

## 2. 本轮实际加载 / 使用的能力

实际加载：

```text
dbs-good-question
dbs-decision
spec-first-ai-engineering
```

实际读取的上游方法论：

```text
C:/Users/Administrator/dbskill_claude_platform_design_kb/01_命令体系/03_需求拷问端演化.md
```

实际读取的项目文件：

```text
README.md
ROADMAP.md
00_知识库设计规范/05_索引与变更记录/知识库索引_v0.1.md
项目目录结构 / 文件数量 / git 状态
```

本轮未调用：

```text
gmgn-swap
gmgn-cooking
Codex
GitHub issue creation
cronjob
```

---

## 3. 当前项目结构观察

项目顶层现在同时存在三类内容：

```text
A. 已归位的正式知识库目录：00-12、99、specs、scripts、tests、.github、skills。
B. 历史资料 / 原始资料目录：文章库_中文Markdown、原始HTML备份、多语言关键词补充资料、指标代码库_源码与说明、meme庄家控盘语义补充资料。
C. 早期根目录文件 / 旧阶段目录：第一控盘区成本中枢回收模型_*.md、策略分类与准备清单.md、知识库迭代机制_v0.1、策略完善_v0.2。
```

这说明项目不是没有结构，而是：

```text
新结构已经建立；旧资料和早期成果仍以历史形态保留；两者之间的“承接说明”不够显性。
```

---

## 4. 当前重要功能模块地图

| 模块 | 功能 | 当前状态 | 主要文件 | 问题 |
|---|---|---|---|---|
| 项目入口 | 让人和 Agent 知道从哪里开始 | 已建立 | README、ROADMAP、AGENTS、SOURCE_OF_TRUTH | 未显性引用 V0.5 上游方法论 |
| 真源和索引 | 确定文件权威性和位置 | 已建立 | SOURCE_OF_TRUTH、知识库索引、变更记录 | 依赖人工维护，缺 index validator |
| V0.5 需求拷问 | 慢速需求拷问、理论补全、非侵入接入 | 部分承接 | demand_theory_evolution_loop、loop_agent_contract、V0.5 承接清单 | Project Continuation Brief 未落地 |
| 样本标注 | 让理论可反证 | 已启动 | 07_样本标注、CSV、FCZ_B/C/D | FCZ_B 未完成，C/D 需图表派生结构 |
| GMGN 字段扩展 | 样本标注数据源字段 | 已产出 | GMGN样本标注字段扩展_v0.1.md | 后续 validator 尚未接入 |
| 资料吸收 | 外部理论 / MQL5 / 论坛吸收 | 已有大量文件 | 05_关键词检索、06_资料吸收、文章库、代码库 | Concept Search Loop 未显性入口化 |
| 规则与反证 | 状态机、反证、消融、成熟度审计 | 已启动 | 09_规则与回测 | 与样本字段和 validator 绑定不足 |
| MQL5 观察器 | 未来指标需求 | 已有需求 v0.2 | 08_指标需求 | MVP 计划未写 |
| Validator / CI | 防止文档漂移 | 已有基础 | scripts、tests、GitHub Actions | 缺 sample record / index / loop report validators |
| GitHub 协作 | issue / PR / CI | PR 中 | .github、10_工程化交接/issues | 本地 issue 草案尚未转真实 GitHub issues |

---

## 5. 已完善但没有充分接入的内容

### 5.1 V0.5 上游方法论

问题：

```text
上游已经很完整，但业务项目没有一张“承接表”，导致后续使用时容易只看到局部文件。
```

本轮动作：

```text
新增 V0.5需求拷问理论补全体系承接清单_v0.1.md。
```

仍需：

```text
README / ROADMAP 补一句上游方法论入口。
```

---

### 5.2 GMGN 样本字段扩展

问题：

```text
这是一次真实 Demand Theory Evolution Loop 的业务输出，但后续容易被当成普通样本文档。
```

应该接入：

```text
1. 样本记录 validator。
2. GMGN 真实样本采集 issue。
3. FCZ_B/C/D 后续标注。
4. ROADMAP P1/P2。
```

---

### 5.3 Concept Search Absorption Loop

问题：

```text
项目有 05_关键词检索 和 06_资料吸收，但没有在 GitHub issue 模板中变成标准入口。
```

结果：

```text
遇到新概念时，Agent 可能直接写判断，而不是走“概念解析 → 检索 → 吸收 → 理论回填”。
```

需要：

```text
新增 concept_absorption_task issue 模板或 issue 草案。
```

---

### 5.4 旧资料 / 新结构的桥

问题：

```text
根目录旧文件、策略完善_v0.2、知识库迭代机制_v0.1、文章库、代码库等都有价值，但它们与 00-12 新结构之间靠索引弱连接。
```

不要马上做：

```text
批量搬迁、批量重命名、重构目录。
```

建议慢做：

```text
先做 Project Continuation Brief，说明哪些是当前主线、哪些是历史资料、哪些只读。
```

---

## 6. 当前最重要的结构不清晰点

```text
1. 方法论上游与业务项目边界不够显性。
2. 新结构和旧资料之间缺一张“从哪里继续”的状态图。
3. 样本、反证、GMGN 字段、validator 之间的依赖关系不够显性。
4. Concept Search Absorption Loop 没有入口模板。
5. ROADMAP 还没有把 V0.5 承接、Project Continuation Brief、Concept Absorption 这些列入 P0/P2。
```

---

## 7. 本轮低置信候选解释

### 解释 A：不是没有体系，而是缺“体系导航层”

机制：

```text
项目已经有大量分线和控制文件，但入口文件没有把“功能 → 文件 → 状态 → 下一步”做成稳定导航。
```

如果成立，应该看到：

```text
Owner 需要反复提醒“昨天那套”“之前完善过的那套”，Agent 才能重新找到。
```

当前观察：

```text
符合。
```

### 解释 B：不是方法论不完整，而是缺“非侵入式承接记录”

机制：

```text
V0.5 方法论完整存在于上游，但业务项目只部分承接，没有写清哪些承接、哪些不承接。
```

如果成立，应该看到：

```text
业务项目有 loop 文件，但没有 V0.5 承接清单和 Project Continuation Brief。
```

当前观察：

```text
符合。本轮已补 V0.5 承接清单，Project Continuation Brief 仍待补。
```

---

## 8. 最小补强路径

按 V0.5 慢做，不建议一次性重构。推荐顺序：

```text
Step 1：新增 Project_Continuation_Brief_v0.1.md。
Step 2：README / ROADMAP 增加方法论上游和 Non-invasive Adoption Guard。
Step 3：执行 issue-007，把 loop-agent report schema 做成 validator。
Step 4：执行 issue-005，做 index reference validator。
Step 5：执行 issue-004，做 sample record markdown validator。
Step 6：回到 P1 样本主线，补 FCZ_B 和 C/D 图表派生结构。
```

---

## 9. 本轮是否达成 V0.5 目标

| 检查项 | 结果 | 说明 |
|---|---|---|
| 只处理一个最高价值问题 | 是 | 聚焦“已完善内容未接入整体体系” |
| 读取上游方法论 | 是 | 读取 V0.5 演化文件 |
| 读取项目现实 | 是 | 检查目录、文件数量、README、ROADMAP、索引 |
| 不做快节奏重构 | 是 | 未移动旧文件，未重构目录 |
| 输出落文件 | 是 | 本慢审计 + V0.5 承接清单 |
| 更新索引和变更记录 | 待完成 | 本文件写完后执行 |
| 运行 validate_all | 待完成 | 本文件写完后执行 |
| 形成下一轮入口 | 是 | Project Continuation Brief 是下一步 |

---

## 10. 下一个最高价值 Owner 问题

```text
是否先新增 Project Continuation Brief，把“当前项目从哪里继续、哪些文件是主线、哪些是历史资料、哪些是方法论上游”固定下来？
```

推荐默认：

```text
是。先做 Project Continuation Brief，再做 validators；否则质量门会检查文件，但 owner 仍然会觉得整体结构散。
```
