# FCZ_B_0001 样本记录

> sample_class：B
> sample_subclass：B1
> symbol：
> timeframe：
> rule_version：v0.1
> reviewer：pending
> review_date：pending

---

## 1. 样本基础信息

```text
sample_id：FCZ_B_0001
sample_class：B
sample_subclass：B1
symbol：
chain_or_market：
exchange_or_dex：
timeframe：
sample_start_time：
sample_end_time：
reviewer：pending
review_date：pending
rule_version：v0.1
sample_source：real_pending
is_real_sample：true
```

---

## 2. 图表与原始资料

```text
source_chart_url：pending_real_chart_url
raw_data_path：pending_real_raw_refs
screenshot_path：
相关截图：pending
相关备注：等待真实图表与 GMGN 原始 refs
```

---

## 3. 事前结构标注

### 第一控盘区

```text
first_control_zone_detected：pending
fcz_high：
fcz_low：
fcz_mid：
fcz_start_time：
fcz_end_time：
fcz_bars：
fcz_volume_share：
poc_level：
vah_level：
val_level：
```

### 第一波与深洗

```text
first_impulse_done：pending
first_impulse_high：
first_impulse_return：
retracement_ratio：
deep_washout_detected：pending
sweep_detected：pending
sweep_grade：
sweep_reclaim_bars：
```

### 成本锚接受

```text
value_area_reentered：pending
poc_reclaimed：pending
avwap_reclaimed：pending
avwap_confluence_count：
best_respected_avwap_anchor：
pullback_holds_anchor：pending
```

### 二次启动 / SMC 辅助

```text
first_bounce_detected：pending
second_push_confirmed：pending
bos_after_reclaim：pending
fvg_after_reclaim_detected：pending
order_block_retest_holds：pending
```

---

## 4. 触发与结果

```text
entry_rule_triggered：pending
triggered_group：
entry_time：
entry_price：
stop_price：
max_favorable_excursion_R：
max_adverse_excursion_R：
gross_realized_R：
net_realized_R_after_cost：
win_loss：
failure_reason：
final_classification：pending
```

---

## 5. 风险评分

```text
death_market_score：pending
bull_trap_score：pending
failed_reclaim_score：pending
distribution_score：pending
liquidity_trap_score：pending
narrative_decay_score：pending
commercial_actionability_score：pending
```

---

## 6. 审计问题

```text
would_rule_have_triggered_ex_ante：pending
was_boundary_moved_after_outcome：pending
was_anchor_changed_after_outcome：pending
is_failure_explained_by_new_rule：pending
new_rule_needed：pending
can_simpler_rule_explain_it：pending
```

### 6.1 这个样本挑战了哪个假设？

```text
- 模型符合但失败
```

### 6.2 是否需要修改规则？

```text
pending
```

---

## 7. 审计结论

```text
审计结论：pending
下一步动作：保留规则 / 降级规则 / 新增观察字段 / 暂停深化 / 进入消融实验
```

---

## 8. CSV 同步检查

必须同步更新：

```text
07_样本标注/反证样本库_v0.1.csv
```

至少同步字段：

```text
sample_id
sample_class
sample_subclass
symbol
timeframe
rule_version
entry_rule_triggered
triggered_group
net_realized_R_after_cost
win_loss
failure_reason
final_classification
would_rule_have_triggered_ex_ante
was_boundary_moved_after_outcome
was_anchor_changed_after_outcome
commercial_actionability_score
notes
```
